name = input('Enter your name: ')
s2 = input('Enter a character or a string: ')
count = name.count(s2, 0, len(name))
print('The number of occurrences of the character or string in (2) in the string in (1):', count)
